package function.search;

import Model.Shortcut;
import Model.ShortcutEntity;

import static Model.Key.ALT;
import static Model.Key.CTRL;
import static Model.Key.SHIFT;
import static Model.Key.UP_OR_DOWN_ARROWS;

public class ClassChanges extends ShortcutEntity {

    private String example = "Do changes on bottom and ask for changes";

    public ClassChanges() {
        super("Search inside class", new Shortcut(CTRL,ALT, SHIFT, UP_OR_DOWN_ARROWS));
    }























































































































































































    private String lineFound;
}
