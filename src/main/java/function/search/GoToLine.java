package function.search;

import Model.Shortcut;
import Model.ShortcutEntity;

import static Model.Key.CTRL;
import static Model.Key.G;
import static Model.Key.SHIFT;

public class GoToLine extends ShortcutEntity {

    private String example = "Go to line 200";

    public GoToLine() {
        super("Search inside class", new Shortcut(CTRL, G));
    }























































































































































































    private String lineFound;
}
